 const Box = ({x,y}) => {
    return (
        <div
        style={{
            display: "inline-block",
            marginLeft: x,
            marginTop: y
        }}
        />
    )
 }
 export default Box